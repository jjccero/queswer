<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gzu.queswer.dao.AnswerDao">
    <insert id="insertAnswer" useGeneratedKeys="true" keyProperty="aid">
        INSERT INTO t_answer (answer_time, uid, qid, answer, anonymous)
        VALUES (#{answer_time}, #{uid}, #{qid}, #{answer}, #{anonymous})
    </insert>
    <update id="updateAnswer">
        UPDATE t_answer
        SET answer=#{answer},
            modify_answer_time=#{modify_answer_time},
            anonymous=#{anonymous}
        WHERE aid = #{aid}
    </update>
    <delete id="deleteAnswerByAid">
        DELETE
        FROM t_answer
        WHERE aid = #{aid}
    </delete>
    <select id="selectAnswerbyAid" resultType="com.gzu.queswer.model.Answer">
        SELECT aid,qid,uid,answer,answer_time answer_time,anonymous
        FROM t_answer
        WHERE aid = #{aid}
    </select>
</mapper>